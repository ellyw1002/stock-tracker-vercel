// import { put, takeEvery, call, select } from 'redux-saga/effects';
// import { addStock } from '../api/fetchStock.api';
// import { addStockFulfilledAction, addStockFailedAction } from '../actions/addStock.action';

// add it later, after finish with screenshot, and decide how to display stock info separately
// const response = yield call(searchStock, action.payload);

// function* addStockAsync(action){
//     if (!response) {
//         yield put(addStockFailedAction());
//     } else {
//         yield put(addStockFulfilledAction(response.));
//     }
// }

// export function* watchAddStock() {
//     yield takeEvery('addStock', addStockAsync);
// }




